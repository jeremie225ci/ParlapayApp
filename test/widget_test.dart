// Este archivo contiene pruebas de widget para tu aplicación.
// Las pruebas de widget se utilizan para verificar que la UI funciona como se espera.
// Para ejecutar estas pruebas, usa el comando: flutter test

// Desactivamos temporalmente las pruebas mientras desarrollamos
// ignore_for_file: all

import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:mk_mesenger/main.dart';

void main() {
  // Desactivamos temporalmente esta prueba mientras desarrollamos
  // Puedes eliminar el comentario cuando estés listo para implementar pruebas
  /*
  testWidgets('Counter increments smoke test', (WidgetTester tester) async {
    // Build our app and trigger a frame.
    await tester.pumpWidget(const MyApp(
      firebaseInitialized: true,
      rapydInitialized: true,
    ));

    // Verify that our counter starts at 0.
    expect(find.text('0'), findsOneWidget);
    expect(find.text('1'), findsNothing);

    // Tap the '+' icon and trigger a frame.
    await tester.tap(find.byIcon(Icons.add));
    await tester.pump();

    // Verify that our counter has incremented.
    expect(find.text('0'), findsNothing);
    expect(find.text('1'), findsOneWidget);
  });
  */
}
